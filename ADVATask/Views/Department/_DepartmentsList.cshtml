@model IEnumerable<ADVATask.ViewModels.DepartmentVM.DepartmentVM>
@{
    int SerialNum = 0;
}
<table class="table table-sm table-hover table-bordered cell-border compact text-center">
    <thead>
        <tr><th scope="col" colspan="5" style="font-weight:bold; font-size:1.5rem;">Departments</th></tr>
        <tr>
            <th scope="col">#</th>
            <th scope="col" style="white-space:nowrap">Name</th>
            <th scope="col" style="white-space:nowrap">Manger</th>
            <th scope="col" style="white-space:nowrap">Employees Count</th>
            <th scope="col" style="width:1px; white-space:nowrap">Actions</th>
        </tr>
    </thead>
    <tbody id="Department-list">
        @if (Model != null && Model.Count() > 0)
        {
            @foreach (var Item in Model)
            {
                SerialNum = SerialNum + 1;
                <tr id="Department-@Item.DepartmentID">
                    <td>
                        @SerialNum
                    </td>
                    <td>@Item.Name</td>
                    <td>
                        @Item.Manger
                    </td>
                    <td>
                        @Item.EmployeesCount
                    </td>
                    <td>
                        <div class="d-flex justify-content-center align-items-center">
                            <button class="btn btn-icon btn-outline-hover-brand btn-sm btn-font-brand edit-item"
                                department-id ="@Item.DepartmentID"
                                department-name="@Item.Name"
                            >
                                <i class="fa fa-edit"></i>
                            </button>
                            <button class="btn btn-clean btn-md btn-icon-md btn-icon TrashIcon ActionBtn delete-item"
                                id="TrashBtn"
                                department-id ="@Item.DepartmentID"
                            >
                                <i class="fas fa-trash"></i>
                            </button>
                        </div>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr><td colspan="5" align="center" style="color:gray;font-size:2em">None</td></tr>
        }
    </tbody>
</table>